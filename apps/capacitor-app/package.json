{
  "name": "dayorder-mobile",
  "version": "1.0.0",
  "description": "日序 - 个人管理系统移动端 (Capacitor)",
  "main": "index.js",
  "scripts": {
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "build:android": "npx cap sync && npx cap open android",
    "build:ios": "npx cap sync && npx cap open ios",
    "serve": "npx serve www -p 3000",
    "dev": "npx serve www -p 3000 --cors"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/ios": "^8.4.2",
    "@capacitor/local-notifications": "^8.2.1",
    "capacitor-sqlite": "^2.2.0"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
